# Local LLM model weights — far too large for git (download/place manually)
*.gguf

# Python caches
__pycache__/
*.pyc

# Runtime caches
lora_meta_cache.json

# LoRA/embedding preview thumbnails — local only, not for the public repo
web/previews/
