# Completions

Task completion docs: `YYYY-MM-DD-task-name.md`, one per finished task.

NEVER auto-loaded (excluded in `.claudeignore`). Read only when explicitly asked
to review past work. Use `.claude/templates/completion-template.md`.
